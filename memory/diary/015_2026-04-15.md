<!-- L0: Session 015 — PharmOrder VPS hardening, OOM diagnosis, sync fixes, DJI mic, Codex collab -->
# Session 015 — 2026-04-15

## Что сделано

### PharmOrder VPS — диагностика и hardening
- **Диагностировали OOM**: 8 убийств PharmOrder за 3 дня (13-15 апреля). Причина — Steam Sniper (240MB) + PharmOrder растёт до 500MB + 0 swap на 961MB VPS
- **Steam Sniper удалён с VPS**: оба сервиса (dashboard + bot), /opt/steam-sniper/. Бэкап в `runtime/steam-sniper-vps-backup/`
- **Swap 1GB добавлен**: /swapfile, persistent через fstab
- **MemoryMax=450M**: guardrail в systemd unit pharmorder.service
- **sklit_cache.db.bak удалён**: 110MB мёртвый бэкап
- **VPS креды сохранены**: `runtime/vps-creds.env` (gitignored), больше не спрашиваем пароль

### Sync client — retry fix (мамин ноут)
- **Найден mtime баг**: `_last_pr_all_mtime` ставился ДО upload. Если upload падал — retry не было
- **Тот же баг** в sync_order_history() и sync_sozvezdie()
- **Пофикшен sync_standalone.py** на съёмном диске (E:\новый склит смнк\sklit_syncV3\) — все 3 места
- Мама подтвердила: заявки отправляются, прайс обновился

### Дубликаты выгрузок — диагноз
- Причина: confirm после записи в DBF не доходит (интернет) → повторная запись
- Решили подождать — OOM был главным триггером, теперь устранён
- Dedup (inflight с lease) готов как план, применим если дубли повторятся

### Codex коллаборация
- Написан полный брифинг для Codex: `~/Desktop/PHARMORDER_CODEX_BRIEFING.md`
- Codex дал ревью: подтвердил диагнозы, нашёл второй mtime баг (history), раскритиковал auto-confirm (справедливо), предложил streaming upload
- Итоговый consensus: mtime fix + inflight queue + streaming + MemoryMax

### DJI Mic Mini + аудио
- DJI подключён через USB-ресивер, спарен, выставлен как default mic через pycaw/COM
- Проблема: передатчик не был спарен с ресивером (синий = не подключён, зелёный = ок)
- Стандартный микрофон: выкрутил на 100% + boost +30 дБ
- VoiceType: убрал из автозагрузки по ошибке → вернул. Ollama убрал из автозагрузки
- Рекомендация: для стационара USB-конденсаторник (Fifine K669B ~1500-2000₽), DJI — для аптеки

## Решения и выводы
- OOM был корневой причиной всех "тупок": прайсы не обновлялись, дубли выгрузок, сайт "не обновлено"
- Архитектуру менять не нужно — точечные reliability фиксы
- На мамином ноуте стоит **другая версия** sync (sync_standalone.py, не sync_client.py) — с дельта-синком, Созвездием, русским выводом

## Ошибки сессии
- Убрал VoiceType из автозагрузки без спроса — пользователь вернул. Не трогать startup без явного запроса
