---
session: "003"
date: 2026-03-20
duration: ~3h
context_used: ~40%
---

# Session 003 — 2026-03-20

## Что делал

### NotebookLM починен
- Auth протух, `notebooklm login` падал (Chrome lockfile)
- Нашёл workaround: через Playwright MCP навигировать на notebooklm.google.com, залогиниться, экспортнуть куки через `context.storageState()`
- Обновил куки на VM (gcloud compute scp)
- Обновил notebooklm-py 0.3.3 → 0.3.4 на VM
- Записал feedback memory + обновил video skill

### Video skill переписан
- Основной путь для YouTube: NotebookLM (не yt-dlp — тот требует куки)
- Добавлен `PYTHONIOENCODING=utf-8` для Windows cp1251
- Auth recovery через Playwright MCP

### Видео-анализ через NotebookLM
- "Build an API in 2026" (Olly Rosewell) — полный саммари через NotebookLM
- Работает: create notebook → add source → ask → parse answer

### Дайджест вайбкодеров (1171 сообщений)
- Вытащил через Telethon → split на 2 файла → NotebookLM
- Два промпта: первый жирный, второй профессиональный (сигналы + ссылки + дискуссии)

### Funding Scanner — анализ расхождений
- Сравнил оригинал (tablefundthe) vs наш через Playwright
- Вытащил live данные из обоих панелей
- Главная проблема: EdgeX fundingRate vs forecastFundingRate
- Codex дал ревью моего анализа — "слишком уверенный, нужен verifier"
- Решили не фиксить одной строкой, а делать верификатор

### Инфраструктура — ops.sh и verify починены
- ops.sh ROOT bug: `scripts/` → корень репо
- ruff + pyright добавлены в 7 tools как dev-зависимости
- verify.md переписан под реальные ops.sh команды
- verify command переписан
- Pre-commit git hook: tests + secrets + lint (автоматически)

### CLAUDE.md переработан
- Сжат 140 → 95 строк по best practices (vibemeta, potapov.dev)
- Позитивные формулировки, "When uncertain" секция
- Path-scoped rules: python-tools.md, security.md, commands-and-skills.md
- Убраны хвосты (npm refs, 80% coverage, "не коммить в main")

### GSD установлен
- `npx get-shit-done-cc --claude --global` — 40+ команд
- Context monitor в statusline (% заполнения контекста)
- Не обкатан на задаче — нужна multi-step задача

### Permissions
- Добавлены `mcp__playwright__*` и `mcp__exa__*` в глобальные разрешения
- Команды: prerequisites checks добавлены в heartbeat, tg-digest, dispatch, screenshot

### Cleanup
- runtime/ — очищен (мёртвые файлы funding, pharmorder, tg-monitor-inspect)
- research/personal-style-ai.md — удалён (мёртвый)
- Все 17 commands + 15 skills проаудированы — все работают

## Наблюдения
- NotebookLM — мощная штука для анализа, но auth на Windows мучительный
- ops.sh был сломан с момента создания — никто не проверял
- CLAUDE.md best practices: <200 строк, path-scoped rules, hooks > текстовые правила
- GSD context monitor полезен — видно сколько контекста съели

## Решения
- EdgeX фикс отложен — нужен verifier, не одна строка
- GSD обкатка — при следующей multi-step задаче
- Cook (CLI оркестратор) — интересен, но пока не ставим
