<!-- L0: Session 016 — Steam Sniper v2.0 полный билд (GSD), деплой, фикс цен/картинок, Codex коллаборация -->
# Session 016 — 2026-04-13 (вечер)

## Что сделано

### Steam Sniper v2.0 — полный milestone через GSD
- **GSD new-milestone** — инициализация v2.0 "Зеркало lis-skins"
- **Research** — 4 параллельных ресёрчера (Stack, Features, Architecture, Pitfalls) + синтез
- **Requirements** — 22 требования в 6 категориях (Refactoring, Catalog, Lists, Cases, PWA, Extension)
- **Roadmap** — 6 фаз (5-10), phases 6+7 параллельны
- **Execution** — все 6 фаз выполнены:
  - Phase 5: Dashboard Split (1373→141 строк, 11 ES модулей, hash router)
  - Phase 6: Category Parser (15 категорий, 57 тестов) + /api/catalog
  - Phase 7: Dual Lists (user_lists таблица, POST/DELETE/GET /api/lists)
  - Phase 8: Catalog UI (карточки, sidebar, heart/star toggle, favorites/wishlist табы)
  - Phase 9: Cases Tab + PWA (manifest, service worker, nginx config)
  - Phase 10: Chrome Extension (Manifest V3, content script для lis-skins)

### Деплой и пост-деплой фиксы
- **Deploy на VPS** — все файлы, systemd restart, оба сервиса active
- **Картинки** — lis-skins JSON не содержит image. Решение: ByMykel/CSGO-API (15003 записей), data/image_cache.json, fallback lookup через rfind("(") для wear suffix
- **Цены** — множитель 1.034→1.0314 (калибровка по реальной цене lis-skins)
- **Русский поиск** — локальный словарь _RU_EN_DICT (80+ терминов), fallback на Steam Market API
- **Service Worker** — cache-first для static файлов блокировал обновления JS. Обновлён CACHE_NAME v1→v2

### Codex коллаборация
- **Передал задачу Codex** — написал детальный CODEX_TASK.md (проект, инфра, SSH, итерации, проблема)
- **Codex итерация 1** — архитектурный фикс: RUB как first-class data (не пересчитывать из USD при каждом запросе). Миграции БД, price_rub в price_history/watchlist/alerts
- **Codex итерация 2** — live rate calibration через Chrome extension. POST /api/internal/rate-samples. Extension читает RUB цену со страницы lis-skins → шлёт sample → сервер калибрует rate. Fallback: formula (CBR * multiplier)
- **Codex репо**: D:\code\2026\3\steam-sniper (отдельная копия)

### Прочее
- **Smoke test субагентов** — researcher, max-transcriber, tg-digest-reader — все работают после рестарта
- **Seedance 2.0 ресёрч** — для Klink. Jimeng $10/мес (китайский), Dreamina $18-84/мес, Atlas Cloud API $0.022/сек

## Решения и инсайты
- GSD workflow на полном milestone — работает. 6 фаз за сессию, параллельные phases 6+7
- Codex сильнее в архитектурных рефакторингах (RUB first-class, rate samples). Claude сильнее в скорости итераций и деплое
- Service Worker cache-first — ловушка при активной разработке. Нужен version bump или stale-while-revalidate
- ByMykel API — бесценный ресурс для CS2 item images. Но wear variants не имеют отдельных image (только base skin)

## Метрики
- ~35 коммитов в steam-sniper repo
- 22 requirements covered
- 8 планов executed
- 15003 image cache entries
- 80+ RU→EN dictionary terms
- 96-98 tests passing (Codex repo)

## Настроение
Продуктивная сессия. Много итераций на пост-деплой фиксы (картинки, цены, кеш). Codex показал себя хорошо на архитектурных задачах.
