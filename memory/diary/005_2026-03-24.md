---
session: "008"
date: 2026-03-24
duration: ~2.5h
---

# Session 008 — PharmOrder fallback, VoiceType streaming STT experiments

## Что сделано

### PharmOrder Local Fallback
- VPS Timeweb упал на 3ч (15:00-18:00) прямо в окно заказа — заявка не ушла
- Создал `~/Desktop/PharmOrder-Local/` — полный локальный fallback:
  - server.py, db.py, inventory.py, barcode_resolver.py, static/
  - Свежие базы с VPS (sklit_cache 100MB, order_history 26MB, sozvezdie 22MB)
  - `АВАРИЙНЫЙ_ЗАПУСК.bat` — один клик, localhost:8000
  - `sync_local.py` — синк заявок на localhost вместо VPS
  - `update_databases.py` — обновление баз через SFTP
- Убрал apteka.ru orders (кнопка + ~500 строк JS + ~500 строк Python) — и локально, и на VPS
- Фикс таймзоны: TZ=Europe/Moscow в server.py (VPS + локалка), пересчитал 62 записи order_history UTC→MSK
- VPS перезапущен, active, работает

### VoiceType Streaming STT (эксперименты)
- Реализовал StreamingSTTClient (stt_streaming.py) — streaming recognize с Chirp 3
- Интегрировал в AudioRecorder (on_chunk callback) и controller
- Эксперимент 1: streaming + interim results → быстро (2.19с) но теряет часть текста
- Эксперимент 2: streaming + polling interim 2с → всё ещё теряет
- Эксперимент 3: streaming + полный finish → 8-9с, медленнее чем batch (5-6с)
- Эксперимент 4: SUPERSHORT endpointing → не помогло
- Эксперимент 5: chirp_2 batch → 400 error, несовместим
- **Вывод: streaming STT с Chirp 3 — тупик. Модель не realtime.**
- Откатился на chirp_3 batch — стабильная работа

### VoiceType Fast Mode улучшение
- Смягчил filler removal в fast_clean: убрал "ну", "вот", "слушай", "смотри", "значит", "блин" из списка
- Оставил только чистые паразиты: короче, типа, как бы, эм, эээ, ммм
- Результат: fast mode теперь не калечит осмысленную речь

## Что НЕ сработало
- Streaming STT с Chirp 3 медленнее batch
- Chirp 2 несовместим с текущим конфигом (400 error)
- Interim results содержат только часть речи для длинных записей

## Память обновлена
- feedback_telegram-via-vm.md — читать TG всегда через VM
- feedback_just-do-it.md — не спрашивать, делать
- project_pharmorder-local-fallback.md — fallback детали
- MEMORY.md — VPS TZ, fallback ссылка, TG feedback

## Следующая сессия
- **Groq Whisper** — главный кандидат на ускорение STT (<1с вместо 3с)
- VoiceType обкатка fast mode после смягчения
- PharmOrder-Local скопировать на мамин ноут
