[project]
name = "tg-bridge"
version = "0.1.0"
description = "Telegram → Claude Code bridge"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "beartype>=0.19",
]

[dependency-groups]
dev = [
    "ruff>=0.9",
    "pyright>=1.1",
]
