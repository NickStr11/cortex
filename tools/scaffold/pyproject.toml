[project]
name = "cortex-scaffold"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "beartype>=0.18.0",
]

[dependency-groups]
dev = [
    "pyright>=1.1.396",
]

[tool.pyright]
typeCheckingMode = "strict"
pythonVersion = "3.12"
